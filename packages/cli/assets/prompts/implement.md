# implement

Implement the verified plan in the assigned worktree.

Use repository conventions, focused tests, and evidence-based validation. Do not run live providers, SDK matrices, or high-count matrices unless explicitly approved.

## Result

Summarize the implementation outcome and any remaining blockers.

## Evidence

Use repository files, tests, command output, and SSOT references as evidence.

## Changes

List changed files and keep changes scoped to the verified plan.

## Test results

Report exact checks run, their result, and any skipped checks with reasons.

Return YAML for the `implement` prompt contract.

Use skill: autokit-implement
Use skill: autokit-question
